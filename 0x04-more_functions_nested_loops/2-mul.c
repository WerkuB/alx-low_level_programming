#include "main.h"
/**
 * mul - multiply two integers
 * Return: product of two numbers
 * @a: first integer
 * @b: second integer
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
